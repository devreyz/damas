export class Board {
  constructor(size) {
    this.elem = document.createElement("div")
    this.size = size
  }
  render() {
    this.elem.textContent = "Tabuleiro"
    return elem
  }
  // Desenha o tabuleiro
  show() {
    
  }
}